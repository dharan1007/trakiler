name: Android Runner Smoke
on:
  push:
    branches:
      - android-runner-smoke-20260920
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Runner
        run: |
          echo runner-ok
          java -version
          which adb || true
